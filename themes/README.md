# Catalog chrome themes

These files skin the main registry (`index.html`) only. They are not design pages.

To add a theme, read [`../theme-toggle.md`](../theme-toggle.md) and add **one** pack when the user asks: a `themes/<id>.css` file, a registry row, and a stylesheet `<link>` in `index.html`.
